version: "3.6"
services:
  lb:
    container_name: "tw_lb"
    restart: always
    build:
      context: ./haproxy
    ports:
      - 80:80
  service_register:
    container_name: "tw_service_register"
    image: consul:latest
    ports:
      - "8500:8500"       
